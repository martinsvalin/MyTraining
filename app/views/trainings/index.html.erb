<h1>Trainings</h1>
<% if @current_user %><p>Welcome <%= @current_user.name %><%=@current_user.id%></p><% end %>
<%#= render :partial => "/shared/navigation" %>
<div class="status">
    <strong><%=@current_user.total_points%></strong> points
</div>
<!--<button><span>Tidigare</span><small>Läs in föregående vecka</small></button>-->
<section>
<% @trainings.each do |training| %>
    <article>
        <header class="clearfix">
            <time class="day" datetime="<%= training.start_time %>">
                <span><%= training.start_time.to_s(:day) %></span><span><%= training.start_time.to_s(:month_short) %></span>
                <%= training.start_time.to_s(:time)%>-<%= training.end_time.to_s(:time)%>
            </time>
            <h1><%= training.title %></h1>
        </header>
        <div class="training">
        <% if workout = training.workout_for_person(@current_user) %>
            <div class="training-item clearfix">
                <span class="name"><strong><%= workout.points %></strong> poäng </span>
                <%= link_to(toggle_button(true), workout, :method => :delete, :class => "enable-disable", "data-status" => "enabled")%>
            </div>
        <% else %>
            <%= form_for training.workouts.new(:person => @current_user) do |f| %>
                <%= f.hidden_field :person_id %>
                <%= f.hidden_field :training_id %>
                <div class="training-item clearfix">
                    <%= link_to(toggle_button(false), workout, :method => :delete, :class => "enable-disable", "data-status" => "disabled") %>
                </div>
            <% end %>
        <% end%>
            <span><%= training.location %></span>
        </div>
        <!--<footer>
            <%= link_to 'Show', training %>
            <%= link_to 'Edit', edit_training_path(training) %>
            <%= link_to 'Destroy', training, :confirm => 'Are you sure?', :method => :delete %>
        </footer>-->
    </article>
<% end %>
</section>
<!--<button>Senare</button>-->
<%= link_to 'New Training', new_training_path %>
